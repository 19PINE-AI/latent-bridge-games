"""Run the text-bridge (T) baseline on a configured game.

Slow model produces text strategic guidance every ~1s; fast model consumes it as
extra context. No training — pure prompting + zero-shot fast-model action prediction.

Status: skeleton, wired up after Stage A.
"""
from __future__ import annotations


def main():
    raise NotImplementedError("Wired up Week 2 after Stage A baseline is established.")


if __name__ == "__main__":
    main()
