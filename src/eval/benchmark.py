"""Four-strategy benchmark harness.

Runs F, T, L (and optionally S, O) on a configured game set with multiple seeds and
episodes per (strategy, game, seed) cell. Outputs per-cell results to results/ as JSON.
"""
from __future__ import annotations


def main():
    raise NotImplementedError("Eval harness: wired up in Week 3.")


if __name__ == "__main__":
    main()
